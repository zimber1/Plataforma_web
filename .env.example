# Example environment variables for local docker-compose and CI
# Copy to .env and fill with real values when running locally

# MongoDB connection for services
MONGO_URI=mongodb://mongo:27017/plataforma

# OpenAI key for catalog-service (if used)
OPENAI_API_KEY=

# Frontend build-time API URL
VITE_API_URL=http://localhost:3000
